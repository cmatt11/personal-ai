"""Personal AI: a local-first, offline-capable AI assistant.

Runs locally with Ollama, optionally uses an online LLM, stores all data
locally (SQLite + vector memory), and uses internet tools when available.
"""

__version__ = "1.0.0"
